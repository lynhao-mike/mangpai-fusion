# Historical Statement Record Coverage

生成时间：`2026-06-12T15:52:19Z`

## 1. 范围

- 扫描范围：`cases/*` 下正式案例目录；排除 `_TEMPLATE` 与 `raw_feedback`。
- 审计对象：`statement_index.json` 与 `statement_records.json`。

## 2. 覆盖率统计

| 指标 | 数量 |
|---|---:|
| 总案例数 | 127 |
| 有 `statement_index.json` 数量 | 127 |
| 有 `statement_records.json` 数量 | 127 |
| 缺失 `statement_records.json` 数量 | 0 |
| 迁移前有 `statement_records.json` 数量 | 127 |
| 本轮补建后有 `statement_records.json` 数量 | 127 |

## 3. 结论

本轮覆盖率为 `100.0%`。
