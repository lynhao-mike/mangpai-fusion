# ============================================================
# requirements-dev.txt · v1.2 开发与测试依赖
# ============================================================
# 由 Track-H 在 W2 阶段建立。
# 安装：pip install -r requirements-dev.txt
# 仅包含运行 pytest tests/ 所必须的依赖。
# ============================================================

pytest>=7.0,<9.0
PyYAML>=6.0
