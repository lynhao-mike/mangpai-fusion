# 问真 invalid_ganzhi_chars 样本核查结果

## 汇总

- 已修复数量：7
- 待人工确认数量：0
- 规则误判数量：0
- 新增可用于案例分析的样本数量：7

## 逐条结果

| 样本ID | 异常原因 | 修复动作 | 修复后状态 | 是否可进入案例分析流程 |
|---|---|---|---|---|
| RF-2026-000286 | 排盘文本中出现非天干字符 `王`，结合十神、纳音与柱位上下文，应为 `壬` 的 OCR 误识别。 | 戊辰丙辰癸丑王子 → 戊辰丙辰癸丑壬子；将疑似 OCR 字形 `王` 修正为天干 `壬`，将地支误字 `西` 修正为 `酉`。 | 已修复待分析 | 是 |
| RF-2026-000452 | 排盘文本中出现非天干字符 `王`，结合十神、纳音与柱位上下文，应为 `壬` 的 OCR 误识别。 | 戊寅王戌丙辰辛卯 → 戊寅壬戌丙辰辛卯；将疑似 OCR 字形 `王` 修正为天干 `壬`，将地支误字 `西` 修正为 `酉`。 | 已修复待分析 | 是 |
| RF-2026-000396 | 排盘文本中出现非天干字符 `王`，结合十神、纳音与柱位上下文，应为 `壬` 的 OCR 误识别。 | 甲申丁卯丙午王辰 → 甲申丁卯丙午壬辰；将疑似 OCR 字形 `王` 修正为天干 `壬`，将地支误字 `西` 修正为 `酉`。 | 已修复待分析 | 是 |
| RF-2026-000663 | 排盘文本中出现非天干字符 `王`，结合十神、纳音与柱位上下文，应为 `壬` 的 OCR 误识别。 空亡字段中出现非地支字符 `西`，上下文为 `申西`，应为 `申酉` 的 OCR 误识别。 | 庚辰戊子王子辛丑 → 庚辰戊子壬子辛丑；将疑似 OCR 字形 `王` 修正为天干 `壬`，将地支误字 `西` 修正为 `酉`。 | 已修复待分析 | 是 |
| RF-2026-000763 | 排盘文本中出现非天干字符 `王`，结合十神、纳音与柱位上下文，应为 `壬` 的 OCR 误识别。 | 王午辛亥辛巳己亥 → 壬午辛亥辛巳己亥；将疑似 OCR 字形 `王` 修正为天干 `壬`，将地支误字 `西` 修正为 `酉`。 | 已修复待分析 | 是 |
| RF-2026-000422 | 排盘文本中出现非天干字符 `王`，结合十神、纳音与柱位上下文，应为 `壬` 的 OCR 误识别。 | 戊寅王戌辛丑丙申 → 戊寅壬戌辛丑丙申；将疑似 OCR 字形 `王` 修正为天干 `壬`，将地支误字 `西` 修正为 `酉`。 | 已修复待分析 | 是 |
| RF-2026-000123 | 排盘文本中出现非天干字符 `王`，结合十神、纳音与柱位上下文，应为 `壬` 的 OCR 误识别。 空亡字段中出现非地支字符 `西`，上下文为 `申西`，应为 `申酉` 的 OCR 误识别。 | 王午甲辰戊申庚申 → 壬午甲辰戊申庚申；将疑似 OCR 字形 `王` 修正为天干 `壬`，将地支误字 `西` 修正为 `酉`。 | 已修复待分析 | 是 |
