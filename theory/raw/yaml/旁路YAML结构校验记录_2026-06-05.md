# 旁路 YAML 结构校验记录

> 校验时间：2026-06-05
>
> 状态：审查态记录；不接入生产引擎。

## 1. 校验对象

- [`theory/raw/yaml/ziping_candidate_rules_2026-06-05.yaml`](ziping_candidate_rules_2026-06-05.yaml)
- [`theory/raw/yaml/tiaohou_ditiansui_candidate_rules_2026-06-05.yaml`](tiaohou_ditiansui_candidate_rules_2026-06-05.yaml)
- [`theory/raw/yaml/domain_weight_profile_2026-06-05.yaml`](domain_weight_profile_2026-06-05.yaml)
- [`theory/raw/yaml/adjudication_contract_example_2026-06-05.yaml`](adjudication_contract_example_2026-06-05.yaml)

## 2. 校验内容

本次只读结构校验覆盖：

1. 子平与滴天髓候选规则顶层 `status` 是否为 `review_draft`；
2. 所有候选规则 `status` 是否均为 `candidate`；
3. 子平规则 ID 是否均以 `ZP-CAND-` 开头；
4. 滴天髓规则 ID 是否均以 `DTS-CAND-` 开头；
5. 领域权重矩阵每个功能域合计是否为 `1.0`；
6. 裁判契约样例是否包含必要字段。

## 3. 校验命令

```bash
python -c "import yaml, pathlib; root=pathlib.Path('theory/raw/yaml'); zp=yaml.safe_load((root/'ziping_candidate_rules_2026-06-05.yaml').read_text(encoding='utf-8')); dts=yaml.safe_load((root/'tiaohou_ditiansui_candidate_rules_2026-06-05.yaml').read_text(encoding='utf-8')); w=yaml.safe_load((root/'domain_weight_profile_2026-06-05.yaml').read_text(encoding='utf-8')); c=yaml.safe_load((root/'adjudication_contract_example_2026-06-05.yaml').read_text(encoding='utf-8')); assert zp['status']=='review_draft' and dts['status']=='review_draft'; assert all(r['status']=='candidate' and r['id'].startswith('ZP-CAND-') for r in zp['rules']); assert all(r['status']=='candidate' and r['id'].startswith('DTS-CAND-') for r in dts['rules']); assert all(abs(sum(v.values())-1.0)<1e-4 for v in w['weights'].values()); ex=c['example']; [ex[k] for k in ['domain','primary_judgement','involved_experts','confidence','feedback_state','weight_profile','evidence_items','arbitration_reason']]; print('Bypass YAML structural checks OK')"
```

## 4. 校验结果

```text
Bypass YAML structural checks OK
```

## 5. 结论

- 当前旁路 YAML 不仅语法可解析，也满足本轮最小结构检查。
- 这些检查仍属于审查态，不等同于正式 schema 验证。
- 进入生产代码前，仍需把本检查固化为只读测试用例。
